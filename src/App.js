import firebase from 'firebase'
require('firebase/auth')
import { auth } from 'firebase'


function App() {
  console.log(auth);

  return (
    <div className="App">
      <h1>h</h1>
    </div>
  );
}

export default App;
